# CheckApp 0.1.1

## Fixes

- Speeds up opening `Uusi kuitti` after entering the Fennoa `Ostot` view.
- Checks visible text, accessibility labels, titles and links before falling back to slower polling.

## Signing

- This local build is ad-hoc signed.
- Apple Developer ID signing and notarization require a valid Developer ID Application certificate in Keychain.
